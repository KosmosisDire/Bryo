type Test
{
    bool stuff = true
}

fn RunTest(bool* test)
{
    Print("Running test...\n")
    if (test[0])
    {
        Print("Test stuff is true\n")
        test[0] = false
    }
    else
    {
        Print("Test stuff is false\n")
    }
}

fn RunTests(Test* test)
{
    test[0].stuff = true
    RunTest(&test[0].stuff)
    RunTest(&test[0].stuff)
}

fn Main: f32
{
    Test test = new Test(true)
    RunTests(&test)

    f32 stuff = 1.0;
    
    return 0.0
}