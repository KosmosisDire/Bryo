type Test
{
    var stuff = 10

    -- TODO: Fix setter HLIR generation
    -- TODO: Fix backing field generation
    var Health = 10
    {
        get => 10
        set
        {
            stuff += 1
        }
    }

    fn Test
    {
        stuff += 10
    }
}

fn Main_f32_
{
    var t = new Test()
    t.stuff += 10
    return t.stuff
}